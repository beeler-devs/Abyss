# Local defaults for Bridge v1 demos
BACKEND_WS_URL=ws://localhost:8080/ws

# Server-side model credentials (set locally; never commit real values)
ANTHROPIC_API_KEY=
CURSOR_API_KEY=

# Optional bridge pairing override (server)
BRIDGE_PAIRING_TTL_MS=300000

# Notes:
# - Set server runtime variables in /Users/bentontameling/Dev/VoiceBot2/server/.env
# - Keep all secrets local only; do not commit populated .env files.
