# BridgeCore

Headless Swift package used by macOS app and CLI.

Responsibilities:
- outbound WebSocket connection
- bridge tool execution (`bridge.exec.run`, `bridge.fs.readFile`)
- workspace policy enforcement
- output truncation + command timeout
